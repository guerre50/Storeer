// Generated by CoffeeScript 1.6.3
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  define(["jquery", "underscore", "backbone", "App", "text!templates/storee.html"], function($, _, Backbone, app, template) {
    var StoreeView, _ref;
    return StoreeView = (function(_super) {
      __extends(StoreeView, _super);

      function StoreeView() {
        _ref = StoreeView.__super__.constructor.apply(this, arguments);
        return _ref;
      }

      StoreeView.prototype.className = 'storee-summary storeer-col col6 square';

      StoreeView.prototype.template = _.template(template);

      StoreeView.prototype.events = {
        'click': 'click',
        'dragstart': 'onDragStart',
        'dragend': 'onDragEnd'
      };

      StoreeView.prototype.initialize = function() {
        return _.bindAll(this);
      };

      StoreeView.prototype.onDragEnd = function(event) {
        this.$el.removeClass('dragging');
        return app.vent.trigger('drag-end:storee');
      };

      StoreeView.prototype.onDragStart = function(event) {
        this.$el.addClass('dragging');
        event.originalEvent.dataTransfer.setData("storee", JSON.stringify(this.model.toJSON()));
        return app.vent.trigger('drag-start:storee');
      };

      StoreeView.prototype.click = function() {
        return app.vent.trigger('open:storee', this.model);
      };

      return StoreeView;

    })(Backbone.Marionette.ItemView);
  });

}).call(this);
